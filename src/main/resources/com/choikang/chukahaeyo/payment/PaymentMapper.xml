<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.choikang.chukahaeyo.payment.PaymentMapper">
    <!-- 결제 정보 삽입 -->
    <insert id="insertPayment" parameterType="com.choikang.chukahaeyo.payment.model.PaymentVO">
        INSERT INTO Payment (payDate,
                             payNo,
                             payAmount,
                             memberID,
                             receiptURL,
                             merchantUid, canceledAt, failReason, cancelReceiptURL)
        VALUES (#{payDate},
                #{payNo},
                #{payAmount},
                #{memberID},
                #{receiptURL},
                #{merchantUid},
                #{canceledAt}, #{failReason}, #{cancelReceiptURL})
    </insert>


</mapper>